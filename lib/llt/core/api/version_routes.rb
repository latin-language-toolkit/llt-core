module LLT
  module Core::Api::VersionRoutes
    def add_version_route_for(route, params)
      dependencies = params[:dependencies]

require 'pry'; binding.pry
      self.class.get("#{interpolation}/version") do

      end
    end
  end
end
